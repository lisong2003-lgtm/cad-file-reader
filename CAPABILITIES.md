# CAD File Reader 能力总览

## 读取与转换

- DWG/DXF/DWT 直接读取；`read_cad.sh` 提供全量实体树和 SVG 渲染，适合小图。
- `cad_scan.sh` 是大图低内存路径：文字、图层、块名、关键词、图框、构件编号、根数口径和几何候选。
- `convert_dwg.sh` 使用内置转换器生成 DXF；支持批量、实体类型过滤和覆盖控制。
- 大文件默认走入口闸和内存看门狗；14MB 以上避免盲目全量解析。

## 标注与解释

- 提取平法集中标注、截面、箍筋、抗震等级、保护层、连接方式、接头率、附加筋和建筑做法关键词。
- `cad_interpret.sh` 绑定图框/构件范围，输出规范引用、规则候选、梁实例拆分和净跨/支座候选解释。
- 自动推断标注为候选或推测，保留原文与坐标证据，不冒充设计确认。
- 梁线候选长度只作图面几何证据；不计算混凝土体积、面积、材料量或工程量。

## 规范/图集辅助

- `cad_normative.sh` 从图纸引用匹配 `rules/standards.json` 元数据索引。
- 输出索引状态、现行/旧版/待确认、专业、知识库相对入口或在线入口。
- 汇总缺少的设计输入（混凝土等级、抗震等级、保护层、做法表、防火等级等）和多图框参数冲突。
- 边界：只做资料定位与风险提示，不复制规范全文，不判定条文合规。

## 装饰识图层

- 从 `cad_scan` 详情 JSON 提炼视图、比例、单位、房间、门窗和做法候选；混凝土等级不误判为窗号，缺做法或缺单位转入待复核。
- 输出闭合房间边界、门窗到墙线/房间归属、做法到节点详图索引；缺几何、跨房间或截断结果转入复核。
- 按闭合房间边界分段墙面为 clear/opening 证据段，绑定显式墙高证据；缺墙高、墙高冲突、非轴对齐或无效边界转入复核。
- 识别主吊顶、检修口、灯具带、造型、梁/降板影响和吊顶标高候选；附属分区按包含关系挂到主分区。
- 识别楼梯、坡道、台阶初稿，保留显式展开/投影面积、分项材料、级数、坡度和图层几何证据；只作待确认初稿。
- 识别外墙分格、保温、防火隔离带、女儿墙内侧和立面饰面候选，保留材料、厚度、标高和图层几何证据；不输出面积或材料量。
- 每条候选带原文、图层、坐标、图框证据；只出中间数据，不直接出工程量。

## 安装识图层

- 识别安装图纸类型、专业系统、路由段、设备、标注、立管和连通性候选。
- 建立系统拓扑候选：闭合环、分支、开放端点和孤立段；开放端点只标记复核原因。
- 识别竖向路由候选：把立管编号/竖向文字关联到同文件、同图框的最近路由段，保留关联距离、方向和证据；只有竖向几何图层时进入 `review`。
- 只输出识图候选和证据；不输出安装工程量、材料量、损耗、造价或结算量。安装算量交给 `anzhuang-suanliang` 等专项 skill。

## 钢结构识图层

- 识别钢结构图纸类型、结构系统、构件、截面、连接、材料、涂装、节点、轴网/标高和几何候选。
- 识别钢柱、钢梁、桁架、支撑、檩条、拉条、墙梁、平台、钢楼梯、栏杆、预埋件、节点板和加劲肋候选。
- 识别 H 型钢、槽钢、角钢、圆管、方矩管、C/Z 型檩条和钢板截面，以及常见钢材材料候选。
- 提取钢构件图层线段、端点吸附、开放端点、汇聚节点和孤立段候选；长度只作单段图面证据。
- 只输出识图候选和证据；不输出钢材重量、面积、长度汇总、材料量、造价或结算量。

## 市政识图层

- 识别道路平面/纵断面/横断面、路面结构、交叉口、排水管网、管道纵断面、检查井、桥梁、涵洞、挡墙、隧道、交通和道路照明等图纸类型。
- 识别道路、桥隧、管网、挡墙、边坡和综合管廊等构件候选。
- 提取桩号、标高、`X/Y` 坐标、管径、坡度、尺寸、比例单位和规范引用证据。
- 提取混凝土、沥青、水泥稳定碎石、级配碎石、管材、灰土、土工布、防水和防腐等材料候选。
- 只输出识图候选和证据；不输出面积、长度汇总、数量、材料量、造价或结算量。

## 测量候选层

- `cad_measure.sh` 读取 `cad_scan` 详情或装饰/安装/钢结构/市政识图 JSON，输出长度、面积、体积候选。
- 每条候选保留 `value`、`unit`、`value_drawing_units`、`scale`、`space`、`scale_applied`、`basis`、`method`、`formula`、`status`、`confidence`、`review_reason`、`source_id` 和证据。
- 闭合多边形用鞋带公式；显式文字测量值可直接采用；外接框面积只作定位证据；体积只在显式体积或面积×显式厚度/高度时生成待复核候选。
- 缺比例/单位时保留图面数值并转 `review`；`model` 空间只按坐标单位换算，只有 `paper/layout` 空间且比例明确时才应用 `1:N`。
- 所有输出固定 `final_quantity=false`；扣减、重叠、做法、损耗、分账、清单量和材料量交给专项算量 skill。

## 几何识图与证据

- 几何提取包括梁线、墙线、房间边界、洞口、轴网/标高、端点和连通性候选；保留文件、图框、图层、坐标和原文证据。
- 净跨、支座、构造层和系统拓扑只做候选解释，并给出复核原因。
- 结构混凝土/钢筋算量、梁板柱墙算量流水线已迁到 `tujian-suanliang`（本机目录 `shangwu-suanliang`）。
- 本技能不输出最终轴线面积、构件体积、混凝土方量、钢筋量、材料量或结算量；只输出带 `final_quantity=false` 的测量候选。

## 交付与复核

- Markdown/JSON/CSV 报告；重要识图流水线生成 manifest 和 SHA-256。
- 报告必须说明证据来源、自动推断、需回图项和授权全文核对项。
- 重复查询复用已有 scan/detail 索引；不改已有锁定基线。

## 硬边界

- 不替代翻样、下料、清单计价、结算、设计合规审查和规范条文解释。
- 不输出最终工程量、体积、面积、材料量、造价或结算量。
- 公司项目路径、内部结果和规范全文不得进入技能发布内容。
